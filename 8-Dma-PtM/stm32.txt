例如：GPIOB->ODR |=0<<0(寄存器操作B0的引脚左移0位） 

stm32的位带操作

//（1bit位带区会膨胀成4个字节）
位带区的一个位，对应位带别名区的四个字节

//外设位带区与外设位带别名区的地址转换
AliasAddr=0x42000000+（A-0x40000000）*8*4+n*4
一个字节8位（*8），一个位膨胀后4个字节（*4），
A:表示我们要操作的那个位所在寄存器的地址
n：位号


gipo的idr/odr，gpio的输入输出的寄存器



启动文件的说明  

1.初始化堆栈指针
	配置栈：在需要的时候修改栈(-->变量）的大小
	配置堆：（主要用于动态内存分配）。
	

2.设置pc指针（Reset handler）

3.初始化中断向量表

4.配置系统时钟不

5.调用c库函数_main,最终到c中


